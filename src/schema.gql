# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type User {
  id: ID!
  name: String!
}

type Conversation {
  id: ID!
  participants: [User!]!
}

type Message {
  id: ID!
  sender: User!
  receiver: User!
  content: String!
  conversation: Conversation!
}

type Query {
  getUserConversations(userId: Int!): [Conversation!]!
  getConversationMessages(conversationId: Int!): [Message!]!
  test: String!
  getUserById(id: Int!): User!
}

type Mutation {
  createConversation(userIds: [Int!]!): Conversation!
  sendMessage(senderId: Int!, receiverId: Int!, conversationId: Int!, content: String!): Message!
  createUser(name: String!): User!
}